/**
 * Shared interpreter matching settings used by the request map and profile
 * summary. Keep this value aligned with the matching rules until per-user
 * radius preferences are persisted in the database.
 */
export const INTERPRETER_MATCHING_RADIUS_KM = 25;
